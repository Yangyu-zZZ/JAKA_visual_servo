# 贡献指南

欢迎贡献本项目！为了保持代码质量和一致性，请遵循下列流程：

1. Fork 本仓库并在 fork 上创建 topic 分支（例如 `feat/xxx` 或 `fix/yyy`）。
2. 遵循现有代码风格（尽量保持 PEP8），在提交前运行 linters。
3. 提交清晰的 commit message，并在 PR 描述中说明改动原因与测试方法。
4. 对重要逻辑添加单元测试（如果适用）。
5. 创建 PR，并在 PR 中引用 issue（若有）。

代码规范建议：
- 使用 type hints（可选但推荐）
- 为公共 API 写文档字符串（docstring）
